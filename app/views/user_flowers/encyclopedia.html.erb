<div class="flower-encyclopedia-container">
  <h1>🌸 花図鑑 🌸</h1>

  <% if @encyclopedia_flowers.any? %>
    <div class="flower-list">
      <% @encyclopedia_flowers.each do |user_flower| %>
        <div class="flower-card">
          <%= image_tag user_flower.flower.bloom_image_path,
                        alt: user_flower.flower.name,
                        class: "flower-image" %>

          <h3 class="flower-name">
            <%= user_flower.flower.name %>
          </h3>

          <p class="flower-date">
            咲かせた日: <%= l user_flower.created_at.to_date, format: :long %>
          </p>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="no-flowers-message">
      まだ満開の花がないみたい。たくさん記録して花を咲かせよう！🌱
    </p>
  <% end %>

  <%= link_to "トップページへ戻る", root_path, class: "btn-back-to-home" %>
</div>
